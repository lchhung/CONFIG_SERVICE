eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
    instance:
    #In production, this hostname will be replace with the server name
      hostname: localhost
